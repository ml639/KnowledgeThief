<!-- <% form_tag resources_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:tag] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %> -->

<div id="leftColumn">
<% if @resources.empty?%>
      <div id="postResults">
    
    <p class="noResults">Sorry, we currently have no results for "<%= params[:tag] %>", you should contribute some!</p>
    
    </div>
<% else %>
  <% @resources.each do |learningresources| %>
 
    <div class="resultWrapper"> <!-- result wrapper -->
        <div class="voteBox">
	<% unless current_user %>
        <img src="/assets/triangle.png" style="position:absolute; top:0; left:12px;">
	<% else %>
	<%= link_to image_tag('/assets/triangle.png', :style => "position:absolute; top:0; left:12px;"), vote_resource_path(learningresources, type: "up"), method: "post" %>
	<% end %>
        <%= learningresources.reputation_for(:votes).to_i %>
	<% unless current_user %>
        <img src="/assets/bottomTriangle.png" style="position:absolute; bottom:0; left:12px;">
	<% else %>
	<%= link_to image_tag('/assets/bottomTriangle.png', :style => "position:absolute; bottom:0; left:12px;"), vote_resource_path(learningresources, type: "down"), method: "post" %>
	<% end %>        
	</div>
        <div class="resourceThumb">
         <%= link_to image_tag('/assets/thumbex.JPG'), "#" %>
        </div>
        
        <div class="resourceCentralWrapper">
        <div class="resourceTitle">
        <%= link_to learningresources.title, learningresources.link, :target => "_blank" %><br />
        </div>
    
        <div class="resourceDescription">
        <%= learningresources.description %>
        </div>
    
    <div class="subOptions">
    <a href="#">share</a>  <a href="#">save</a>  <a href="#">manage</a>  <a href="#">flag</a>
    </div>
    
 </div> <!-- end central wrapper -->

</div> <!-- end result wrapper -->
 <% end %>
<% end %>



</div>


