$(document).ready(function() {
	$('.modalPopup').fancybox({
	    maxWidth    : 400,
	    maxHeight   : 400,
	    fitToView   : true,
	    autoSize    : false,
	    openEffect  : 'none',
	    closeEffect : 'none',
	});
});