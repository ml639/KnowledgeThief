$(document).ready(function() {
	$('.modalPopup').fancybox({
	    maxWidth    : 300,
	    maxHeight   : 250,
	    fitToView   : true,
	    autoSize    : false,
	    openEffect  : 'none',
	    closeEffect : 'none',
	});
});

$(document).ready(function() {
	$('.modalPopup_resource').fancybox({
	    maxWidth    : 300,
	    maxHeight   : 350,
	    fitToView   : true,
	    autoSize    : false,
	    openEffect  : 'none',
	    closeEffect : 'none',
	});
});

$(document).ready(function() {
	$('.modalPopup_question').fancybox({
	    maxWidth    : 300,
	    maxHeight   : 250,
	    fitToView   : true,
	    autoSize    : false,
	    openEffect  : 'none',
	    closeEffect : 'none',
		scrolling   : 'no',
	});
});